import { getHelpArticles } from "../helpContent";
import HelpArticleCard from "./HelpArticleCard";
type Props = { onOpenArticle: (slug: string) => void };
export default function HelpHome({ onOpenArticle }: Props) { const articles = getHelpArticles(); const categories = [...new Set(articles.map((article) => article.category))]; return <section className="help-container help-home"><div className="help-hero"><p className="help-eyebrow">Guías para profesionales</p><h1>Centro de Ayuda</h1><p>Encontrá guías paso a paso para configurar Turnelia y gestionar tu agenda.</p></div><div className="help-section-heading"><p className="help-eyebrow">Empezá por acá</p><h2>Guías disponibles</h2></div><div className="help-cards">{articles.map((article) => <HelpArticleCard key={article.slug} article={article} onOpen={() => onOpenArticle(article.slug)} />)}</div><div className="help-categories"><p className="help-eyebrow">Explorá por tema</p>{categories.map((category) => <span key={category}>{category}</span>)}</div></section>; }
